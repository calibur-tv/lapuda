{
  "scripts": {
    "docs:make": "php artisan api:docs --use-version v1 --output-file ./vuepress/api/v1/index.md",
    "docs:dev": "vuepress dev vuepress",
    "docs:build": "vuepress build vuepress",
    "docs:publish": "npm run docs:make && npm run docs:build",
    "prettier": "prettier",
    "pretty": "npm run prettier -- app/**/*.php --write"
  },
  "devDependencies": {
    "@prettier/plugin-php": "^0.5.0",
    "prettier": "^1.14.0"
  }
}
